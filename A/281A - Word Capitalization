#include <iostream>
#include <cctype>
#include <string>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	string word;
	while(cin>>word){
		word[0] = toupper(word[0]);
		cout<<word<<'\n';
	}
	return 0;
}

Time complexity: O(1)
